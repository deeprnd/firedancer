name: On Main Push
on:
  push:
    branches: [main]
jobs:
  migration_policy:
    uses: ./.github/workflows/migration_policy.yml

  book:
    uses: ./.github/workflows/book.yml
    secrets: inherit
    permissions:
      contents: read
      pages: write
      id-token: write
