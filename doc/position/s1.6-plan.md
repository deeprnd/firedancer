Plan
Redefine the test pyramid and make the commands mean one thing each.
just test-unit-tk becomes fully offline and mocks every external surface.
just test-integration-tk becomes deterministic boundary integration with the real HttpBackend and local mock LLM/broker servers.
just test-system-tk becomes the real-LLM V1.1 demo lane.

Split src/tickoni/test/integration/allowed_trade.zig by domain and intent.
Keep allowed_trade only for the happy-path allowed trade.
Add separate files for blocked limits, restricted instrument, replay/tamper, and input/policy denials.

Move transport proof into integration, not system.
Replace the current skip-prone model_tile_http.zig live dependency with an OpenAI-compatible mock HTTP server so integration proves request building, HTTP I/O, parsing, and fail-closed behavior without llama.cpp.
Do the same for broker/quote surfaces.

Tighten unit scope.
Unit tests for tkmodl, tkadpt, tktool, tkagnt, replay, thesis normalization, and ticket building should never depend on running servers.
They should use canned mocks and assert policy/budget/validation behavior directly.

Add the documented missing integration scenarios.
At minimum:
allowed_trade
oversized_trade_denied
restricted_instrument_denied
direct_place_denied
malformed_thesis_denied
replay_success
replay_tamper_detected

Add reusable integration test infrastructure.
Create one shared mock OpenAI-compatible server and one shared mock broker/market server helper under src/tickoni/test/integration/.
That avoids stuffing HTTP setup logic into each scenario file.

Rewire build.zig and justfile.
integration-test should run the split domain integration files plus the HTTP-backed transport tests.
The current integration-test-live-model step should stop being a core integration gate and move conceptually under system/demo validation.

Make test-system-tk the V1.1 demo proof.
It should run the real llama.cpp flow and exercise the same scenarios with clear output and assertions.
This is where the “one command shows the demo” requirement belongs, not in the offline integration gate.

Update docs after the code moves.
Align [doc/testing/v1-1-investment-demo-integration.md](/home/vicgenin/work/git/tickoni/doc/testing/v1-1-investment-demo-integration.md), [doc/position/s1.6.md](/home/vicgenin/work/git/tickoni/doc/position/s1.6.md), [doc/position/s.1.6-ext.md](/home/vicgenin/work/git/tickoni/doc/position/s.1.6-ext.md), [doc/position/wbs.md](/home/vicgenin/work/git/tickoni/doc/position/wbs.md), [build.zig (line 311)](/home/vicgenin/work/git/tickoni/build.zig:311), and [justfile (line 86)](/home/vicgenin/work/git/tickoni/justfile:86) so the release story matches reality.

Lock the release gates.
   Merge gate:
   just test-unit-tk
   just test-integration-tk
   Opt-in or scheduled:
   just test-system-tk

Recommended execution order
Split the integration files first.
Add shared mock servers.
Convert model_tile_http.zig to HTTP-against-mock integration.
Add the missing denial scenarios.
Rewire build.zig and justfile.
Promote test-system-tk into the real demo lane.
Update docs last.
Key design rule
Integration should prove Tickoni’s boundaries and transport.
System should prove the real external runtime.
Right now those concerns are mixed, which is why the live proof is weak and the docs overclaim.
If you want, I can take this plan straight into implementation and start with the integration file split plus mock-server harness.