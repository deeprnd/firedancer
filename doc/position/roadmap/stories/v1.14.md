# V1.14: Firedancer Process And Shared-Memory Topology

**Status: Planned · Milestone: M2**

**Epic:** #TBD · **Issue:** #TBD

Replace in-process tile threads with supervisor-managed processes communicating
over Firedancer Tango shared-memory links. V1.11 runs tiles as in-process
threads behind a logical topology boundary; this increment makes process
isolation real and is a prerequisite for crash-propagation guarantees,
seccomp/Landlock sandboxing, and production deployment.

Extracted from V1.11.S6.

---

## Product Intent

V1.11 established a governed tile topology with audit, replay, and no-bypass.
The tile boundaries are logical: tiles share a process and communicate over
heap-backed queues. V1.14 converts every tile into a supervisor-managed process
and replaces heap-backed queues with Firedancer Tango `mcache`/`dcache`
shared-memory links. Demo and replay output must be identical across both
exclusive-core and shared-core CPU configs.

## User Story

As a Tickoni operator, I can run each tile as an isolated supervisor-managed
process over Firedancer shared-memory links, so that tile crashes are contained,
CPU placement is validated, and tile boundaries are real rather than logical.

## What The Operator Sees

- process list with one entry per tile, each managed by the supervisor
- shared-memory workspace identifiers per inter-tile link
- CPU affinity assignments per tile in the supervisor configuration
- link health metrics (depth, overrun count, drop count) per link
- crash report with the identity of the failing tile, isolated from sibling tiles

## Success Demo

- [ ] Supervisor starts each tile as a separate process; `ps` shows one process per tile.
- [ ] Events cross tile boundaries through Tango shared-memory links; no heap queue is present.
- [ ] CPU placement fails closed when a configured CPU id is unavailable.
- [ ] A crashing tile does not corrupt or stall a sibling tile's process.
- [ ] Demo and replay output matches under exclusive-core and shared-core configs.

## Release Gate

- `just test-unit-tk` passes with no unexpected skips
- `just test-integration-tk` passes offline against local mocks
- integration test shows tiles as isolated processes over shared-memory links
- CPU placement fails closed for malformed or unavailable CPU ids
- demo and replay results are byte-identical under both core configs

## Non-Goals

- no new tile capabilities or policy rules
- no changes to capability envelopes, audit records, or replay capsule shape
- no changes to the investment demo product path

---

## WBS

### V1.14.S1: Supervisor-managed processes over shared-memory links

**Issue:** #TBD

**Description:** Replace in-process tile threads with supervisor-managed processes, back inter-tile links with Firedancer Tango shared-memory, define link properties including workspace and health metrics, add validated CPU placement, and integration tests.

Tasks:

- V1.14.S1.T1: Replace in-process tile threads with supervisor-managed
  processes.
- V1.14.S1.T2: Back inter-tile links with Firedancer Tango `mcache`/`dcache`
  shared-memory; remove heap-backed queues.
- V1.14.S1.T3: Define each link's producer, consumer, workspace, mode, depth,
  MTU, reliability, overrun, restart, shutdown, and health metrics.
- V1.14.S1.T4: Add validated CPU-placement: exclusive affinity and explicit
  shared-core assignments.
- V1.14.S1.T5: Integration tests for process isolation, shared-memory flow,
  backpressure, crash propagation, shutdown, and deterministic output under both
  exclusive and shared-core configs.

Acceptance:

- Every tile runs in a supervisor-managed process.
- Events cross tile boundaries only through configured shared-memory links.
- CPU placement fails closed for malformed or unavailable CPU ids.
- Demo and replay results match under both exclusive-core and shared-core
  configs.

---

### V1.14.S2: Evidence and quality gate

**Issue:** #TBD

**Description:** Run process-isolation and shared-memory integration tests, export link health metrics through tkmetr, confirm byte-identical demo output across core configs, test crash isolation, add supervisor configuration sample.

Tasks:

- V1.14.S2.T1: N/A — no consumer-facing demo command; isolation is verified
  through integration tests wired to `just test-integration-tk`.
- V1.14.S2.T2: N/A — infrastructure increment, no consumer-facing demo moment.
- V1.14.S2.T3: Add or update `just test-integration-tk` to run process-isolation
  and shared-memory-flow tests.
- V1.14.S2.T4: Update roadmap status in this file to Done when all acceptance
  criteria pass; remove N/A annotations that no longer apply.
- V1.14.S2.T5: N/A — process topology is not fixture-driven; isolation is
  verified through integration tests.
- V1.14.S2.T6: N/A — no case or thesis fixtures required.
- V1.14.S2.T7: Add sample supervisor configuration showing CPU layout, link
  depth, MTU, and workspace identifiers for the V1.11 investment-demo topology.
- V1.14.S2.T8: N/A — this increment does not change audit records.
- V1.14.S2.T9: N/A — no approval path introduced.
- V1.14.S2.T10: Export link health metrics (depth, overrun count, drop count)
  per link through `tkmetr`; export link state through `tkdiag`.
- V1.14.S2.T11: Confirm demo and replay output is byte-identical under both
  exclusive-core and shared-core configs after the topology change.
- V1.14.S2.T12: Add a test that crashes one tile process and confirms sibling
  tiles and the supervisor report the failure without data corruption.
- V1.14.S2.T13: Add focused tests for process isolation, shared-memory flow,
  backpressure, crash propagation, and shutdown.
- V1.14.S2.T14: N/A — forbidden-shell and forbidden-network apply at policy and
  adapter layers, not at process topology.
- V1.14.S2.T15: N/A — no new envelopes introduced.
- V1.14.S2.T16: Add fail-closed tests for malformed workspace identifiers, CPU
  ids that do not exist, and link MTU mismatches.
- V1.14.S2.T17: N/A — no provider configuration surface introduced.
- V1.14.S2.T18: N/A — no adapter manifest introduced.
- V1.14.S2.T19: N/A — no external ingestion or partner review endpoints changed.

Acceptance:

- `just test-integration-tk` passes with process-isolation and shared-memory
  tests included.
- A crashing tile is reported with its identity; sibling tiles are unaffected.
- Demo and replay output is byte-identical under exclusive-core and shared-core
  configs.
- Link health metrics appear in `tkmetr` output for every active link.

## Evidence Gate

- What can the operator do now that they could not before? Each tile runs in an
  isolated process; a tile crash is contained and reported without affecting
  sibling tiles.
- What changed from V1.11? Heap-backed queues replaced with Tango shared-memory
  links; tile threads replaced with supervisor-managed processes.
- What is the demo moment? Integration test shows one process per tile, links
  over shared-memory, CPU placement validation, and crash isolation.
- Which checks are enforced? CPU affinity and workspace assignment fail closed
  for malformed or unavailable configuration.
- What happens on a tile crash? Supervisor reports the failing tile; sibling
  tiles continue; no data corruption.
- Is execution paper-only, draft-only, sandbox, live, or disabled? Unchanged
  from V1.11 — paper-only.
- Which artifacts are needed for partner trust? Supervisor configuration sample,
  link health metrics, crash isolation integration test output.
- Which command closes the increment? `just test-integration-tk` with
  process-isolation suite passing.
- Which fixtures are used? N/A — process topology is verified through
  integration tests, not financial fixtures.
- Are policy and limit decisions visible in audit output? Unchanged from V1.11.
- Can replay run without side effects? Yes; replay output must match under both
  core configs.
- What intentional failure proves failure behavior? Crashing one tile process
  confirms sibling isolation.
