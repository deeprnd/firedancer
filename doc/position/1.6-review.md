No real user-facing thesis input path.
The roadmap says a user can type plain English. Current code uses structured fixtures with pre-filled target amount, market, asset class, sector, account, etc. normalize() validates a populated ThesisInput; it does not parse arbitrary natural language into intent.

No exec-facing deterministic demo command.
S1.6 asks for one local demo command. build.zig only installs tickoni-supervisor as an executable in [build.zig (line 69)](/home/vicgenin/work/git/tickoni/build.zig:69). The V1.1 demo is a test under the live system lane in [v1_1_demo_live.zig (line 265)](/home/vicgenin/work/git/tickoni/src/tickoni/test/system/v1_1_demo_live.zig:265), not a deterministic fixture-mode demo binary.

The agent/demo still bypasses the governed tkmodl orchestrator.
runInvestmentAgent() calls model_backend.call() directly in [agent/mod.zig (line 50)](/home/vicgenin/work/git/tickoni/src/tickoni/tiles/agent/mod.zig:50). The live system test builds a ProviderRequest and calls the HTTP backend directly in [v1_1_demo_live.zig (line 53)](/home/vicgenin/work/git/tickoni/src/tickoni/test/system/v1_1_demo_live.zig:53) and [v1_1_demo_live.zig (line 276)](/home/vicgenin/work/git/tickoni/src/tickoni/test/system/v1_1_demo_live.zig:276). That violates the S1.6 requirement that model work goes through the governed tkmodl boundary.

Trading ticket scope is narrower than WBS.
WBS calls for buy/sell preview and market/limit orders. Current implementation has Side.sell and OrderType.limit enums, but only buildMarketBuyTicket() is implemented in [trade_ticket.zig (line 229)](/home/vicgenin/work/git/tickoni/src/tickoni/schema/trade_ticket.zig:229). No sell preview, no limit order builder, no limit-price-required validation.

Policy is not truly owned by tkpoly in the V1.1 flow.
Basket scope checks are explicitly described as provisional schema-module scaffolding in [basket.zig (line 217)](/home/vicgenin/work/git/tickoni/src/tickoni/schema/basket.zig:217). Ticket policy is inline in ticket construction in [trade_ticket.zig (line 258)](/home/vicgenin/work/git/tickoni/src/tickoni/schema/trade_ticket.zig:258). That is acceptable for a scaffold, not for the architecture promise that tkpoly owns capability decisions.

Audit/replay is improved but still not fully contract-complete.
Zig audit payloads include new model context fields in [schema.zig (line 61)](/home/vicgenin/work/git/tickoni/src/tickoni/tiles/audit/schema.zig:61), but the C/protobuf codec still only carries the older model fields. Replay capsules also still contain symbolic request/adapter hash strings, for example [replay_capsule.json (line 18)](/home/vicgenin/work/git/tickoni/src/tickoni/test/fixtures/investment/replay_capsule.json:18).

Missing S1.6 test-plan scenarios remain.
There are malformed thesis unit/fixture tests in [denied_trade.zig (line 56)](/home/vicgenin/work/git/tickoni/src/tickoni/test/fixtures/investment/denied_trade.zig:56), but not the documented integration scenario. I also do not see a direct trading_order.place bypass-denial integration test; the closest is adapter rejection of a denied ticket in [adapter/backend.zig (line 406)](/home/vicgenin/work/git/tickoni/src/tickoni/tiles/adapter/backend.zig:406).