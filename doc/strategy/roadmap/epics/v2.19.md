# V2.19: Tickoni Native Investment Terminal

**Status:** Backlog  
**Milestone:** M2  
**UI Platform:** Qt 6 desktop application  
**Supported Platforms:** Linux, macOS, Windows  
**Labels:** `epic`, `terminal`, `desktop`, `qt`, `trust`, `audit`, `investing`,
`platform`, `enhancement`

Build Tickoni's first native professional financial terminal: a dense,
keyboard-first Qt desktop application that turns investment intent into a
policy-checked, explainable, and replayable proposal.

The terminal adopts the seriousness, speed, information density, and
function-driven workflow of professional financial terminals. Tickoni's
primary object is the governed financial consequence rather than price
movement, PnL, rank, or trading activity.

The application is a presentation and operator-control surface over `tkapi`.
Financial correctness, policy decisions, proposal identity, audit records,
replay results, model and adapter governance, and execution authority remain
owned by the Tickoni runtime.

V2.19 deliberately proves one complete workflow before expanding into a broad
operations suite:

```text
investment intent
-> normalized mandate
-> basket and ticket
-> policy decision
-> before/after impact
-> proposal or governed paper action
-> audit and replay proof
```

## Product Intent

Tickoni's existing increments prove deterministic investment flows, governed
runtime boundaries, portfolio consequence, audit, replay, and cross-platform
build support. V2.19 turns those capabilities into the first visible product
surface suitable for repeated operator use.

The first terminal release must answer five questions without requiring the
operator to read fixture output, audit JSONL, replay logs, or diagnostic files:

1. What did the user ask Tickoni to do?
2. What proposal did Tickoni produce?
3. Which policy checks allowed, denied, or resized it?
4. What changes in cash, buying power, and exposure if the proposal proceeds?
5. Can the decision and its evidence be reproduced and verified?

The product line for this increment is:

```text
Tickoni turns investment intent into policy-checked, replayable financial proposals.
```

## Strategic UI Direction

V2.19 is a native desktop terminal, not a browser dashboard made to resemble
one.

The product should borrow these professional-terminal qualities:

- persistent desktop application state;
- keyboard-first navigation;
- command-driven function access;
- high-density tables with stable alignment;
- multiple windows for parallel case inspection;
- visible source, freshness, environment, and policy context;
- immediate movement between summary and exact evidence;
- minimal animation and no decorative dashboard behavior.

The product must not imitate Terminal-specific branding, proprietary function
codes, data products, or visual assets. Terminal is a reference for workflow
discipline and information density, not a design template to copy.

## Users And Jobs

- Investment user: enter or inspect an investment thesis and understand the
  bounded proposal before any action.
- CaseOps operator: move quickly between investment cases, policy reasons,
  consequence, and proof.
- Compliance or risk reviewer: inspect exact policy gates, hashes, evidence
  references, and replay outcome.
- Product reviewer: run deterministic allowed, denied, oversized, replay, and
  tamper scenarios from one native application.
- Runtime owner: see a compact terminal/API availability summary sufficient to
  distinguish unavailable data from a policy denial. Full operations
  administration is deferred.

## Success Metrics

- One native Qt application builds and launches on Linux, macOS, and Windows.
- The USD 2,000 V1.1/V1.11 AI-infrastructure scenario renders normalized
  intent, account state, basket, rejected candidates, ticket, policy outcome,
  impact, proposal identity, and replay status.
- The restricted `SOXL` scenario shows the exact denied reason and exposes no
  quote or paper-action affordance for the rejected instrument.
- The USD 25,000 scenario shows the effective maximum or blocking limit and
  disables the direct action.
- Missing, stale, malformed, disconnected, or unavailable state never renders
  as allowed.
- Navigation and local command dispatch respond without perceptible delay.
- The minimum supported viewport preserves mode, freshness, policy decision,
  exact reason, consequence, and action state.
- No UI path bypasses `tkapi`, `tkpoly`, `tkaudt`, `tkrepl`, `tkmodl`,
  `tktool`, `tkadpt`, or future `tkexec` boundaries.
- The same fixture produces the same semantic terminal state on all supported
  platforms.

## Demo Moment

Run the native Tickoni terminal against deterministic local fixture data.

The operator opens the AI-infrastructure case and sees:

- the original thesis and normalized mandate;
- account cash and buying power;
- an allowed basket and paper ticket;
- restricted `SOXL` in the rejected-instruments table;
- exact policy checks and limits;
- before/after cash and exposure;
- proposal, policy, and event identities;
- replay `MATCH` and tamper `CLEAN`;
- a governed `PLACE PAPER` or `SAVE` action only when an existing runtime
  contract permits it.

The operator then switches to the oversized fixture and the terminal shows the
blocking limit, exact reason, and disabled action without changing screens or
opening raw logs.

## Scope

### In Scope

- Qt 6 native desktop application.
- Qt Quick/QML presentation layer with C++ application services and typed data
  models.
- CMake-based build integration and packaging lanes for Linux, macOS, and
  Windows.
- Dense dark terminal shell, trust/status strip, command line, function pages,
  keyboard navigation, and multi-window case inspection.
- Investment safe-action workbench for V1.1/V1.11 deterministic flows.
- Compact policy detail and before/after impact functions using V1.3 semantics.
- Compact audit and replay proof function using existing V1.7/V1.18-style
  fields where available.
- One aggregated, read-optimized investment case view through `tkapi` or a
  fixture-compatible client.
- Allowed, restricted-instrument, oversized-notional, unavailable, replay
  match, and tamper/divergence fixture states.
- Governed save-proposal and paper-placement actions only where an existing
  documented `tkapi` contract already exists.
- Screenshots, fixture examples, command tests, model tests, and cross-platform
  launch evidence.

### Out Of Scope

- Payment, payout, retry, transfer, or beneficiary workflows.
- Combined investment and payment obligation workspace.
- Full CaseOps kanban or case-board workflow.
- Full capability catalog or approval-path administration.
- Adapter management, tile topology, queue inspection, metrics dashboards, or
  diagnostic workbenches.
- Live brokerage order placement or unrestricted execution.
- Live market-data terminal, news, messaging, research, chat, portfolio
  optimization, or autonomous rebalancing.
- Mobile, tablet, or web-client support.
- Plugin architecture, end-user scripting, arbitrary shell execution, or
  unbounded command forwarding.
- Evidence force graphs, general relationship graphs, Monte Carlo displays,
  scatter plots, decorative heatmaps, or other advanced visualizations without
  a proven operator need and a governed runtime data contract.
- Direct model, tool, adapter, broker, store, shared-memory, TigerBeetle,
  DuckDB, Markdown, audit-file, or replay-file access from the Qt application.
- Reuse of Firedancer validator GUI, RPC, plugin, Solana account, or validator
  product schemas as Tickoni terminal contracts.
- New financial policy decisions, approval semantics, capability envelopes,
  tile IDs, ownership rules, queues, workspaces, or runtime links.
- Profit-first hero cards, leaderboards, trader ranks, dopamine-style
  celebration, or gamified trading concepts.

## Architecture And Ownership

The terminal remains outside the deterministic financial runtime:

```text
+-----------------------------+
| Tickoni Qt Terminal         |
| QML views + C++ models      |
+-------------+---------------+
              |
              | HTTP / WebSocket
              v
+-----------------------------+
| tkapi                       |
| CaseOps read/review edge    |
+-------------+---------------+
              |
  +-----------+-----------+----------------+----------------+
  |                       |                |                |
  v                       v                v                v
 tkpoly                  tkaudt           tkrepl           tkmetr/tkdiag
 policy authority        audit truth      replay truth     availability only
```

- `tkapi` is the only terminal boundary for case, evidence, proposal, paper
  action, audit, replay, and compact system availability.
- `tkpoly` remains the only authority for finance-native policy outcomes.
- `tkaudt` remains the source of material event history and audit identities.
- `tkrepl` remains the source of replay match, divergence, capsule, and tamper
  state.
- `tkmodl`, `tktool`, and `tkadpt` remain hard boundaries for model, tool, and
  adapter access.
- Future `tkexec` remains disabled unless a separate approved-execution epic
  explicitly enables a sandbox or live path.
- The terminal may cache presentation data, but cache contents are never a
  source of financial truth.
- A backend disconnect, stale payload, or contract error must transition action
  state to disabled or unavailable.

## Qt Technical Foundation

Use:

```text
Qt 6
+-- Qt Quick / QML              terminal shell and visual composition
+-- Qt Quick Controls           controls with a custom Tickoni style
+-- C++                         application state, API client, commands
+-- QAbstractTableModel         basket, policy, impact, evidence tables
+-- QAbstractListModel          cases, commands, alerts, timeline
+-- HTTP / WebSocket            tkapi transport
+-- CMake                       desktop build and packaging integration
```

### Required Separation

- QML owns layout, binding, focus, transitions, and presentation only.
- C++ owns transport, schema validation, application state, command parsing,
  typed models, retries, disconnect handling, and mutation dispatch.
- QML components must not call HTTP, WebSocket, model providers, adapters,
  files, shell commands, or execution systems directly.
- Financial policy must not be recomputed in QML or C++ terminal code.
- Display models may derive presentation-only values such as formatted text,
  column widths, and style roles from governed semantic state.
- Runtime semantic values must not be inferred from colors, labels, or icon
  state.

### Suggested Source Layout

```text
src/tickoni/terminal/
+-- CMakeLists.txt
+-- app/
|   +-- main.cpp
|   +-- ApplicationController.*
|   +-- WindowManager.*
+-- api/
|   +-- TkApiClient.*
|   +-- FixtureClient.*
|   +-- ApiTypes.*
|   +-- ContractValidation.*
+-- commands/
|   +-- CommandRegistry.*
|   +-- CommandParser.*
|   +-- CommandDispatcher.*
+-- models/
|   +-- CaseListModel.*
|   +-- InvestmentCaseModel.*
|   +-- BasketTableModel.*
|   +-- PolicyTableModel.*
|   +-- ImpactTableModel.*
|   +-- EvidenceTimelineModel.*
+-- qml/Tickoni/
|   +-- Main.qml
|   +-- shell/
|   +-- pages/
|   +-- panels/
|   +-- controls/
|   +-- style/
+-- fixtures/
+-- tests/
```

## Terminal View Contract

V2.19 should not require the frontend to assemble the main case view from many
loosely synchronized calls. Define one aggregated read model for the first
release, with optional detail reads for expanded proof or evidence.

Suggested conceptual response:

```text
InvestmentCaseView
+-- case
+-- environment
+-- source
+-- freshness
+-- intent
+-- account
+-- proposal
+-- ticket
+-- rejected_candidates
+-- policy_decision
+-- policy_checks
+-- impact_summary
+-- action_state
+-- audit_summary
+-- replay_summary
+-- runtime_summary
```

Each major section must carry or inherit:

```text
status
source
as_of
freshness
reason_unavailable
```

Where policy applies, the response must include:

```text
policy_version
outcome
reason_code
reason_text
observed_value
limit_value
scope
```

Where audit or replay applies, the response must include fields already owned
by the runtime, such as:

```text
event_hash
policy_hash
proposal_hash
evidence_refs
replay_capsule_id
replay_status
first_divergent_sequence
first_divergent_field
expected_hash
actual_hash
```

The exact wire schema remains owned by `tkapi`; this section defines the
minimum UI view requirements, not a second policy or audit schema.

## Visual Identity: Midnight Oni

The terminal should feel disciplined, technical, and premium rather than
colorful or decorative.

The visual concept is:

```text
cold machine surfaces
+ blue Tickoni intelligence
+ restrained amber terminal interaction
+ rare vermilion brand seal
+ strict semantic state colors
```

### Color Responsibilities

Brand color and operational state must remain separate.

| Role | Purpose | Suggested value |
| --- | --- | --- |
| Carbon black | Main workspace | `#070A0E` |
| Midnight navy | Standard panel | `#0B1119` |
| Steel navy | Raised or selected surface | `#101923` |
| Dark slate | Subtle divider | `#192431` |
| Blue-gray | Strong divider | `#263445` |
| Cool white | Primary text | `#E7EDF4` |
| Muted steel | Secondary text | `#8D9AAA` |
| Disabled gray | Unavailable text | `#566270` |
| Oni blue | Tickoni identity and active navigation | `#3478D4` |
| Active blue | Focus, selected step, link | `#4A9EFF` |
| Terminal amber | Prompt, function cue, shortcut | `#E8A83E` |
| Wealth vermilion | Rare brand seal or oni detail | `#A9343D` |
| Pass green | Allowed, pass, replay match | `#47A66A` |
| Signal red | Denied, failed, divergence | `#E0525E` |
| Warning amber | Approval, evidence, stale, near limit | `#D69A32` |

### Color Rules

- Blue identifies Tickoni, current focus, selected function, active case,
  evidence navigation, and the current workflow step.
- Terminal amber identifies the command prompt, function mnemonic, keyboard
  shortcut, timestamp emphasis, and small operator cues.
- Wealth vermilion is a rare brand element only: oni eye, horn, seal, premium
  report mark, or equivalent identity detail.
- Wealth vermilion must not mean allowed, profitable, successful, selected, or
  safe.
- Signal red is reserved for operational denial, failure, tamper, divergence,
  and destructive confirmation.
- Green is reserved for allowed, pass, healthy, replay match, and clean proof.
- Warning amber is reserved for approval required, evidence missing, stale
  state, near-limit, or degraded availability.
- Gray means pending, disabled, unavailable, inactive, or not applicable.
- Color is never the only carrier of meaning. Every state also requires text,
  a code, or a distinct symbol.

### Color Proportion

The default workspace should remain predominantly neutral:

```text
80% black, navy, and gray surfaces
15% white and muted information text
 4% Tickoni blue focus and navigation
 1% amber, vermilion, and semantic state accents combined
```

This is a discipline target rather than a pixel-level test. Large saturated
panel fills, rainbow tables, multi-color gradients, glowing borders, and
colored drop shadows are prohibited.

### Status Treatment

Prefer:

- a thin colored left rule;
- compact text status;
- a small square or dot;
- one-pixel underline;
- restrained badge outline;
- direct table-cell state.

Avoid large filled status cards.

Example:

```text
| ALLOWED      green rule + green text
| DENIED       signal-red rule + signal-red text
| APPROVAL     warning-amber rule + amber text
| UNAVAILABLE  gray rule + gray text
```

### Typography

- Use a compact sans-serif for labels and navigation.
- Use a monospace or tabular-number face for values, commands, hashes, IDs, and
  dense tables.
- Align decimals and currency units.
- Use no more than three primary text sizes in the default workspace.
- Use uppercase for short function names and statuses, not for paragraphs.
- Prefer weight, alignment, and spacing before adding another color.
- Fonts must have redistributable licensing appropriate to the selected Qt
  distribution model; no font file is committed without license review.

### Borders, Shape, And Motion

- Thin gray-blue dividers.
- Sharp rectangles or minimal radius.
- No consumer-dashboard card shadows.
- Motion is limited to focus transition, command completion, connection state,
  replay progress, and critical state change.
- No celebratory animation.
- Hidden or background panels must not continuously animate or redraw.

## Information Architecture

Use five top-level functions in V2.19:

```text
CASE
POLICY
IMPACT
PROOF
SYSTEM
```

- `CASE`: default investment workbench.
- `POLICY`: exact checks, limits, observed values, and denied reasons.
- `IMPACT`: cash, buying power, exposure, and concentration before/after.
- `PROOF`: audit identity, evidence, replay, tamper, and divergence summary.
- `SYSTEM`: compact source, freshness, API, fixture/runtime mode, and backend
  availability. It is not a full operations console.

Deferred top-level areas include `PAYMENTS`, `CASEOPS`, `CAPABILITIES`,
`ADAPTERS`, `HEALTH`, and a full `AUDIT` explorer.

## Main Terminal Composition

```text
+------------------------------------------------------------------------------+
| TKNI | PAPER | CASE 493 | POLICY v1.11 | DATA 12s | API OK | REPLAY MATCH   |
+------------------------------------------------------------------------------+
| CASE          POLICY          IMPACT          PROOF          SYSTEM          |
+----------------------+--------------------------------------+----------------+
| INTENT / ACCOUNT     | PROPOSAL                             | DECISION       |
|                      |                                      |                |
| Thesis               | Basket / ticket table                | ALLOWED        |
| Amount               | Included and rejected instruments    | Policy gates   |
| Cash / buying power  | Allocations and estimated cost       | Exact reasons  |
+----------------------+--------------------------------------+----------------+
| IMPACT: cash, exposure, concentration     | PROOF: hashes, evidence, replay   |
+------------------------------------------------------------------------------+
| > CASE 493                                                                  |
+------------------------------------------------------------------------------+
```

Detailed policy, impact, and proof information opens as function pages or
secondary windows. The default case screen must not attempt to display every
available runtime field at once.

## Trust Strip

Replace a decorative market ticker with a persistent trust strip:

```text
MODE PAPER
CASE case_493
ACCOUNT demo_cash_rich
POLICY v1.11
SOURCE FIXTURE or TKAPI
AS-OF 17:36:12 UTC
AGE 12s
API OK
ADAPTER paper_broker:v0
REPLAY MATCH
```

Requirements:

- mode, source, age, policy version, and action authority remain visible;
- stale or disconnected state is impossible to miss;
- market prices are shown only when required by the case contract;
- the strip does not imply a broad live market-data service.

## Command And Function Model

The command line is a first-class navigation and action surface.

Initial commands:

```text
CASE <id>
NEW
POLICY
IMPACT
PROOF
SYSTEM
WHY <instrument>
SAVE
PLACE PAPER
REPLAY
BACK
HELP
```

### Command Rules

- Commands are registered in a typed C++ allowlist.
- QML captures input and renders suggestions; C++ parses and dispatches.
- Navigation commands are local and non-mutating.
- Governed actions call explicit `tkapi` methods only.
- Unknown input never reaches a shell, model, tool, adapter, file path, or
  network endpoint.
- The command registry generates autocomplete and help from one source of
  truth.
- Command history is session-local for V2.19.
- Sensitive commands require a structured confirmation panel.
- Commands unavailable under the current mode, policy, freshness, or contract
  are visible as disabled with an exact reason.

### Paper Placement Confirmation

`PLACE PAPER` must display:

```text
account
mode
ticket_id
proposal_hash
policy_outcome
policy_version
target_notional
estimated_cost
cash_after
replay or evidence state
```

The action remains disabled when any required identity, contract, policy,
freshness, or backend condition is absent.

## Multi-Window Model

V2.19 should establish a desktop window model suitable for future professional
workspaces without implementing a complete workspace manager.

Acceptance scope:

- open a case in a second terminal window;
- independent page and focus state per window;
- shared read-only case data where appropriate;
- one authoritative action-dispatch path prevents duplicate submission;
- restore basic window geometry and last active function;
- preserve correct scaling across multiple monitors.

Full saved workspaces, detachable panels, scripting, and plugin-provided windows
are deferred.

## Cross-Platform And Display Targets

```text
Design target:        1920 x 1080
Minimum supported:    1280 x 720
High-density test:    2560 x 1440
Scaling tests:        100%, 125%, 150%, 200%
Platforms:            Linux, macOS, Windows
Mobile/tablet:        out of scope
```

Requirements:

- The same application and semantic fixtures run on all supported platforms.
- No feature depends on Unix shell commands, path separators, process tools, or
  filesystem layout.
- Keyboard mappings support standard `Ctrl` conventions and macOS `Cmd`
  equivalents where appropriate.
- Numeric columns remain aligned under all supported scale factors.
- At minimum size, secondary details move to function pages or drawers rather
  than hiding or truncating critical state.
- Policy outcome, exact reason, mode, freshness, and action state must never be
  clipped.
- Native window chrome should be retained for the first release unless custom
  chrome proves reliable for resizing, accessibility, high-DPI, and platform
  integration on all three operating systems.

## Accessibility And Input

- Every action is reachable from the keyboard.
- Focus order is deterministic and visible.
- Color is never the only state indicator.
- Status text and table cells expose accessible names.
- Reduced-motion mode disables nonessential transitions.
- Minimum contrast is verified for primary text, secondary text, focus,
  selected rows, and semantic states.
- Mouse use is supported but never required for core case inspection.
- Screen-reader compatibility is tested for the command line, function tabs,
  policy result, and action confirmation.

## Performance Budgets

- Cold launch to usable fixture screen: under 2 seconds on defined reference
  hardware.
- Local command to page navigation: under 50 ms.
- Received API state to visible critical-state update: under 100 ms.
- Scrolling a 10,000-row synthetic table remains responsive.
- A single quote or status change must not rebuild the whole screen.
- Hidden pages must not continuously render.
- Repeated case switching does not produce unbounded memory growth.
- Only one continuous animation may be visible in the default workspace, and
  only when it communicates live connection or replay progress.
- Profiling evidence is collected with Qt tooling for startup, frame timing,
  binding churn, model updates, and memory behavior.

## Security And Fail-Closed Behavior

- Unknown commands are non-mutating and local.
- Unavailable contracts disable actions.
- Stale case data disables mutation according to the documented contract.
- A disconnected backend cannot leave a previously enabled action enabled.
- Denied candidates cannot expose quote, proposal, or paper-placement actions
  that the governed path did not produce.
- The terminal stores no broker, model, adapter, or execution credentials.
- Logs and crash reports redact sensitive case and account fields according to
  the existing diagnostic policy.
- Fixture mode is visibly different from runtime mode.
- Paper mode is visibly different from any future sandbox or live mode.
- No visual style, label, or color can override semantic action state.

## Qt Licensing And Distribution Gate

The Qt licensing and distribution model must be resolved before production UI
implementation proceeds.

Required decision record:

- selected Qt commercial or open-source governance model;
- complete list of Qt modules used and their applicable licenses;
- dynamic versus static linking decision;
- installer, replacement, relinking, notice, and source-offer obligations where
  applicable;
- CI and package-distribution treatment;
- third-party font and icon licensing;
- policy for avoiding accidental use of modules incompatible with the chosen
  distribution model;
- approved migration process if the licensing model changes.

This is an engineering release gate requiring legal review; it is not resolved
by this epic text.

## Existing Product Mapping

### V1.1 / V1.11 Investment Flow

V2.19 renders, without changing semantics:

- natural-language thesis;
- normalized investment intent;
- eligible basket and exact allocations;
- rejected instruments and exact reasons;
- trade ticket and estimated cost;
- account cash, buying power, and effective maximum;
- policy outcome;
- save-proposal or governed paper state;
- proposal identity;
- replay match and deliberate tamper result.

Required deterministic states:

```text
USD 2,000 AI-infrastructure proposal: allowed
SOXL leveraged ETF candidate: denied before quote or paper work
USD 25,000 request: blocked or resized with effective maximum
forged/direct placement attempt: unavailable or denied
replay of captured run: match
modified captured input: first divergence shown
```

### V1.3 Impact Semantics

V2.19 uses a compact subset of the existing consequence model:

- cash before/after;
- buying power before/after;
- sector exposure before/after;
- ETF exposure before/after;
- single-name concentration before/after;
- material display-ready explanations;
- unavailable/evidence-required state when the runtime does not provide an
  impact value.

Payment obligations, beneficiary exposure, rail exposure, payment approval,
and combined investment/payment scenarios are deferred.

### V1.7 / V1.18 Trust And Replay Semantics

V2.19 displays a compact proof summary when the runtime supplies it:

- event hash;
- policy hash;
- proposal hash;
- evidence references;
- actor and agent attribution;
- model route;
- adapter attribution;
- approval or action state;
- replay capsule ID;
- replay match or divergence;
- first divergent sequence and field;
- tamper status.

The terminal does not recompute hashes, reconstruct replay, or validate proof
cryptography in presentation code.

## Story Breakdown

- [ ] V2.19.S0: Qt foundation, view contract, and golden fixtures
- [ ] V2.19.S1: Terminal shell, visual system, commands, and windows
- [ ] V2.19.S2: Investment case workbench and governed action state
- [ ] V2.19.S3: Policy and impact functions
- [ ] V2.19.S4: Proof function, cross-platform packaging, and release evidence

---

## V2.19.S0: Qt Foundation, View Contract, And Golden Fixtures

**Status:** Backlog  
**Epic:** V2.19  
**Labels:** `story`, `qt`, `platform`, `api`, `documentation`, `enhancement`

Establish the Qt application, C++/QML boundary, `tkapi` view contract, fixture
client, typed models, build integration, and licensing decision needed before
feature UI implementation.

### Product Outcome

Implementers have one cross-platform native application foundation and one
stable investment-case view model. UI stories can proceed without inventing
policy, audit, replay, adapter, storage, or execution semantics in QML.

### User Story

As a terminal developer, I want a typed Qt foundation and deterministic view
contract so that every platform and screen consumes the same governed state.

### Scope

- In scope: Qt 6 application target, CMake integration, QML module, C++
  application controller, typed API client, fixture client, contract
  validation, core table/list models, golden fixtures, platform CI targets,
  licensing decision record.
- Out of scope: final visual polish, domain pages, live external dependencies,
  financial-policy changes, new runtime ownership, and direct execution.

### Preconditions And Assumptions

- `tkapi` remains the only UI boundary.
- The V1.1/V1.11 deterministic scenarios provide the required semantic data.
- Missing runtime endpoints may be represented by a documented fixture-backed
  contract, but no UI action may pretend an unimplemented mutation exists.
- Qt licensing and module selection must be approved before release packaging.

### Tasks

- V2.19.S0.T1: Add the Qt 6 desktop application and CMake targets under a
  Tickoni-owned terminal source tree.
- V2.19.S0.T2: Define the QML/C++ ownership boundary and prohibit network,
  filesystem, shell, model, tool, adapter, and execution calls from QML.
- V2.19.S0.T3: Define the aggregated `InvestmentCaseView` read requirements with
  source, freshness, unavailable reason, policy, impact, audit, replay, and
  action-state fields.
- V2.19.S0.T4: Implement a fixture client and a `tkapi` client behind the same
  typed interface.
- V2.19.S0.T5: Add `QAbstractTableModel` and `QAbstractListModel` foundations for
  case, basket, rejected candidate, policy, impact, and evidence data.
- V2.19.S0.T6: Add contract validation that fails closed for missing identities,
  invalid enums, malformed values, stale state, and inconsistent action state.
- V2.19.S0.T7: Add golden fixtures for allowed USD 2,000, denied `SOXL`, blocked
  or resized USD 25,000, unavailable data, replay match, and tamper divergence.
- V2.19.S0.T8: Add Linux, macOS, and Windows configure/build/test CI lanes.
- V2.19.S0.T9: Record Qt licensing, module, linking, distribution, font, and icon
  decisions.
- V2.19.S0.T10: Document local fixture launch and runtime-connected launch.

### Acceptance Criteria

- [ ] The same Qt application target configures and builds on Linux, macOS, and
      Windows.
- [ ] QML imports typed models and application services without owning transport
      or financial logic.
- [ ] Fixture and runtime clients populate the same model interfaces.
- [ ] Every main terminal section has source, freshness, status, and unavailable
      behavior.
- [ ] Invalid or stale action state fails closed and cannot enable mutation.
- [ ] Golden fixtures cover allowed, denied, oversized, unavailable, replay, and
      tamper states.
- [ ] Qt distribution and module use have an approved decision record before
      packaging work proceeds.

### Conditional Acceptance

**Financial capability and policy**

- [ ] Contract examples carry policy version, outcome, reason code, reason text,
      observed value, limit value, and relevant scope.

**Audit and replay**

- [ ] Contract examples carry runtime-owned hashes, evidence references, replay
      capsule, replay status, and divergence fields where available.

**Runtime topology and ownership**

- [ ] No tile, link, queue, workspace, or process ownership changes are made.

**Model, tool, adapter, or execution boundary**

- [ ] UI fixtures contain attribution and governed status only; they contain no
      provider credentials or direct dispatch contract.

**CaseOps API or UI**

- [ ] `tkapi` is documented as the only terminal boundary.

### Evidence Plan

- Demo: launch the Qt shell with each golden fixture.
- Tests: C++ contract validation, fixture parsing, model roles, stale-state,
  malformed-state, and no-action tests.
- Build evidence: Linux, macOS, and Windows configure/build/test logs.
- Documentation: view contract, source layout, run commands, and licensing
  decision record.

---

## V2.19.S1: Terminal Shell, Visual System, Commands, And Windows

**Status:** Backlog  
**Epic:** V2.19  
**Labels:** `story`, `qt`, `terminal`, `design`, `accessibility`, `enhancement`

Deliver the native terminal frame, Midnight Oni visual system, trust strip,
function navigation, command line, keyboard focus model, high-DPI behavior, and
basic multi-window support.

### Product Outcome

Operators can move through a stable, dense, keyboard-first desktop workspace
that feels like a professional terminal while remaining unmistakably Tickoni.

### User Story

As an operator, I want command-driven navigation and persistent context so that
I can inspect cases quickly without losing mode, source, policy, freshness, or
proof state.

### Scope

- In scope: main window, trust strip, five function pages, command input,
  autocomplete, keyboard shortcuts, focus treatment, loading/error/empty/stale
  states, Midnight Oni palette, typography, high-DPI behavior, second case
  window, geometry restoration, accessibility basics.
- Out of scope: payment pages, operations dashboard, detachable arbitrary
  panels, saved workspaces, plugins, scripting, and final advanced charts.

### Preconditions And Assumptions

- S0 typed models and fixtures are available.
- Native window chrome remains the default unless cross-platform custom chrome
  passes all acceptance tests.
- Terminal-style density is a workflow reference, not permission to copy
  proprietary branding or function codes.

### Tasks

- V2.19.S1.T1: Implement the main Qt Quick window and five function destinations:
  `CASE`, `POLICY`, `IMPACT`, `PROOF`, and `SYSTEM`.
- V2.19.S1.T2: Implement the persistent trust strip with mode, case, account,
  policy, source, as-of time, age, API state, adapter attribution, and replay
  state where available.
- V2.19.S1.T3: Implement the Midnight Oni design tokens and custom Qt Quick
  Controls style.
- V2.19.S1.T4: Apply blue to Tickoni identity and active navigation, amber to
  terminal interaction, rare wealth vermilion to brand detail, and strict
  semantic green/red/amber/gray states.
- V2.19.S1.T5: Implement command input, typed autocomplete, command history,
  help, and deterministic unknown-command failure.
- V2.19.S1.T6: Implement keyboard-first focus order, visible focus, shortcuts,
  reduced motion, and accessible names.
- V2.19.S1.T7: Implement responsive behavior for 1280x720 through 2560x1440 and
  scale factors from 100% through 200%.
- V2.19.S1.T8: Implement second-window case opening with independent navigation
  and one authoritative action dispatcher.
- V2.19.S1.T9: Add visual snapshots for core resolutions, scales, and semantic
  states.
- V2.19.S1.T10: Profile startup, frame timing, binding churn, hidden-page work,
  and repeated window/case switching.

### Acceptance Criteria

- [ ] The trust strip, function navigation, active page, and command line render
      without overlap at supported sizes and scale factors.
- [ ] Mode, source, freshness, policy outcome, exact reason, and action state are
      never clipped or hidden.
- [ ] Blue, amber, wealth vermilion, and semantic colors follow the documented
      responsibilities; no large saturated panel fills or decorative gradients
      appear.
- [ ] Every semantic state has text or code in addition to color.
- [ ] Known commands navigate or invoke their documented governed action.
- [ ] Unknown commands remain local, non-mutating, and cannot reach runtime,
      shell, network, model, tool, adapter, or execution paths.
- [ ] All core functions are reachable without a mouse.
- [ ] A second case window can open without duplicating action submission.
- [ ] The fixture screen becomes usable within the startup performance budget.

### Conditional Acceptance

**Financial capability and policy**

- [ ] Shared status components display semantic outcomes but never derive them.

**Audit and replay**

- [ ] Trust-strip replay state is loaded from governed data or fixture state.

**Runtime topology and ownership**

- [ ] No process, tile, link, or queue changes are introduced.

**Model, tool, adapter, or execution boundary**

- [ ] Commands are allowlisted and cannot forward arbitrary text to governed
      providers.

**CaseOps API or UI**

- [ ] All runtime-connected state is received through the S0 client boundary.

### Evidence Plan

- Demo: open the allowed case, navigate every function by keyboard, open a
  second case window, run `HELP`, and demonstrate unknown-command failure.
- Tests: focus order, shortcuts, command parser, command allowlist, stale state,
  color-role snapshot, accessibility labels, and layout snapshots.
- Screenshots: 1920x1080 default, 1280x720 minimum, 2560x1440 high density,
  200% scaling, allowed, denied, stale, and disconnected states.

---

## V2.19.S2: Investment Case Workbench And Governed Action State

**Status:** Backlog  
**Epic:** V2.19  
**Labels:** `story`, `investing`, `qt`, `trust`, `enhancement`

Render the V1.1/V1.11 investment flow as the terminal's default `CASE`
function: thesis, normalized mandate, account, basket, rejected candidates,
ticket, decision summary, and governed action state.

### Product Outcome

Users and operators can see how an investment thesis became a bounded proposal,
which candidates were rejected, and whether the final action is allowed,
blocked, resized, proposal-only, or paper-ready.

### User Story

As an investment user, I want to inspect the complete policy-checked proposal
before action so that I understand both the proposed consequence and the denied
alternatives.

### Scope

- In scope: thesis display/input state, normalized fields, account summary,
  basket table, rejected-candidate rows, ticket summary, policy outcome,
  effective maximum, proposal identity, exact reason, save-proposal state, and
  governed paper-placement state.
- Out of scope: new basket construction, new financial policy, live quotes,
  live brokerage, payment actions, autonomous portfolio management, and direct
  adapter calls.

### Preconditions And Assumptions

- S0 golden fixtures provide the USD 2,000, restricted `SOXL`, and USD 25,000
  states.
- Paper placement is displayed as an enabled mutation only when an existing
  documented `tkapi` contract and current governed action state permit it.
- Without that contract, the terminal remains read-only or save-proposal-only.

### Tasks

- V2.19.S2.T1: Implement intent and normalized-mandate panels for amount,
  currency, market, venues, asset classes, theme, exclusions, and concentration
  preference.
- V2.19.S2.T2: Implement account summary for cash, buying power, remaining daily
  notional, and effective maximum.
- V2.19.S2.T3: Implement a virtualized basket table using a typed table model.
- V2.19.S2.T4: Represent rejected candidates in the same table grammar with
  exact policy code, reason, and unavailable actions.
- V2.19.S2.T5: Implement ticket summary with line items, estimated cost, order
  mode, proposal identity, and paper/proposal state.
- V2.19.S2.T6: Implement the allowed USD 2,000 case.
- V2.19.S2.T7: Implement the restricted `SOXL` denied case with no quote or
  paper-action affordance.
- V2.19.S2.T8: Implement the USD 25,000 blocked or resized case with effective
  maximum and disabled action.
- V2.19.S2.T9: Implement `WHY <instrument>`, `SAVE`, and `PLACE PAPER` command
  handling through the typed action dispatcher.
- V2.19.S2.T10: Implement structured paper-placement confirmation and duplicate
  submission prevention.

### Acceptance Criteria

- [ ] The USD 2,000 fixture shows the original thesis, normalized mandate,
      account, basket, ticket, decision, and governed action state in one case
      workspace.
- [ ] Basket allocations, amounts, prices, states, and reasons remain aligned and
      readable at all supported scale factors.
- [ ] `SOXL` is shown as rejected with the exact restricted-instrument reason and
      no quote, proposal, or paper-placement action generated by the UI.
- [ ] The USD 25,000 case shows the observed amount, effective maximum or limit,
      outcome, exact reason, and disabled direct action.
- [ ] The UI does not treat `allow` alone as sufficient for paper placement;
      required ticket, proposal, account, environment, policy, action identity,
      freshness, and backend state must match the runtime contract.
- [ ] A repeated action attempt cannot produce duplicate submission.
- [ ] A disconnected or stale case disables governed mutation immediately.

### Conditional Acceptance

**Financial capability and policy**

- [ ] Policy state and scope are displayed from governed data; the terminal does
      not recompute allow, deny, approval, or resizing.

**Audit and replay**

- [ ] Proposal, policy, and event identities link to `PROOF` when available.

**Runtime topology and ownership**

- [ ] No topology changes are introduced.

**Model, tool, adapter, or execution boundary**

- [ ] Market, model, and adapter attribution is display-only.
- [ ] Paper placement uses only an existing governed `tkapi` action contract.

**CaseOps API or UI**

- [ ] Case data and actions use the S0 typed client and contract validation.

### Evidence Plan

- Demo: load USD 2,000, `SOXL`, and USD 25,000 fixtures; run `WHY SOXL`; show
  structured paper confirmation or documented disabled state.
- Tests: model roles, row virtualization, allowed case, rejected instrument,
  oversized notional, stale case, disconnect, malformed identity, and duplicate
  submission.
- Screenshots: allowed, restricted, oversized, read-only, and paper-ready states.

---

## V2.19.S3: Policy And Impact Functions

**Status:** Backlog  
**Epic:** V2.19  
**Labels:** `story`, `investing`, `policy`, `impact`, `qt`, `enhancement`

Deliver dedicated `POLICY` and `IMPACT` functions that make exact limits,
observed values, denied reasons, and before/after consequence visible without
advanced charts.

### Product Outcome

Operators can understand why the proposal is allowed or blocked and what it
changes in cash, buying power, exposure, and concentration.

### User Story

As a risk-conscious user, I want exact policy and consequence tables so that I
can evaluate the proposal without trusting a summary label or visual color.

### Scope

- In scope: policy-check table, reason detail, effective limit, observed value,
  scope, before/after cash, buying power, sector exposure, ETF exposure,
  single-name concentration, material explanations, unavailable state.
- Out of scope: policy authoring, optimization, rebalance automation, combined
  payment obligations, scenario simulation, Monte Carlo, scatter plots, and
  decorative heatmaps.

### Preconditions And Assumptions

- Policy and impact fields come from S0 governed view data or fixtures.
- The UI never invents missing before/after values.
- Values unavailable from the runtime display `UNAVAILABLE` or
  `EVIDENCE REQUIRED` with a reason.

### Tasks

- V2.19.S3.T1: Implement the `POLICY` function using a typed table with check,
  result, scope, limit, observed value, reason code, and reason text.
- V2.19.S3.T2: Implement direct navigation from case summary and
  `WHY <instrument>` to the relevant policy row.
- V2.19.S3.T3: Implement the `IMPACT` function using before, after, change, unit,
  and material explanation fields.
- V2.19.S3.T4: Render cash, buying power, technology/sector exposure, ETF
  exposure, and maximum single-name concentration where available.
- V2.19.S3.T5: Add compact summary strips on the case page without duplicating
  full policy or impact tables.
- V2.19.S3.T6: Implement unavailable, stale, evidence-required, near-limit,
  allowed, and denied table states.
- V2.19.S3.T7: Verify color use: principal comparison in blue/gray, semantic state
  only in green/signal-red/warning-amber, optional mandate line in rare
  vermilion only where it cannot be confused with denial.

### Acceptance Criteria

- [ ] Every policy row names the check, outcome, observed value, applicable
      limit or scope, and exact reason where present.
- [ ] The restricted-instrument and oversized-notional reasons are accessible
      directly from the case page and command line.
- [ ] The impact function shows before, after, and change for every available
      governed metric.
- [ ] Missing impact fields are never inferred from account or basket data in
      the terminal.
- [ ] Color is supplementary; status text and numeric values remain sufficient
      to understand every row.
- [ ] No advanced chart is required to close the story.

### Conditional Acceptance

**Financial capability and policy**

- [ ] Policy results are runtime-owned and display-only.

**Audit and replay**

- [ ] Policy and impact snapshot identities or evidence references are shown
      where supplied.

**Runtime topology and ownership**

- [ ] No topology changes are introduced.

**Model, tool, adapter, or execution boundary**

- [ ] No model, tool, adapter, or execution call is introduced.

**CaseOps API or UI**

- [ ] Policy and impact functions use typed S0 models and unavailable-state
      handling.

### Evidence Plan

- Demo: navigate from the allowed case to policy and impact, then switch to the
  oversized and unavailable fixtures.
- Tests: exact policy reasons, observed/limit values, before/after calculations
  supplied by fixtures, missing-field behavior, stale state, and color-role
  snapshots.
- Screenshots: allowed policy, denied policy, near-limit warning, full impact,
  and unavailable impact.

---

## V2.19.S4: Proof Function, Cross-Platform Packaging, And Release Evidence

**Status:** Backlog  
**Epic:** V2.19  
**Labels:** `story`, `audit`, `replay`, `qt`, `release`, `enhancement`

Deliver the compact `PROOF` function, deterministic replay and tamper displays,
platform packages or launch artifacts, performance evidence, and the final
release pack.

### Product Outcome

Reviewers can verify the identity, provenance, evidence, replay status, and
first divergence behind a case from the native terminal, and the same release
behavior is demonstrated on Linux, macOS, and Windows.

### User Story

As a compliance or product reviewer, I want a compact black-box view and
cross-platform evidence so that I can verify what happened without reading raw
audit or replay files.

### Scope

- In scope: event/policy/proposal hashes, evidence references, actor and agent,
  model route, adapter attribution, action state, replay capsule, replay match,
  tamper state, first divergence, compact timeline, platform packaging/launch
  artifacts, performance evidence, screenshots, and release commands.
- Out of scope: hash recomputation in UI, replay execution logic in UI, full
  forensic query system, evidence graph, proof-bundle management portal,
  external reviewer portal, and production auto-update service.

### Preconditions And Assumptions

- Replay and audit state is provided by runtime-owned fixtures or `tkapi` reads.
- The terminal may request a governed replay only if an existing documented
  contract exists; otherwise `REPLAY` selects or refreshes supplied proof state.
- Package format may differ by platform, but semantic behavior and fixtures must
  remain identical.

### Tasks

- V2.19.S4.T1: Implement the `PROOF` function with case identity, event hash,
  policy hash, proposal hash, evidence refs, actor, agent, model route, adapter,
  action state, replay capsule, replay status, and tamper state.
- V2.19.S4.T2: Implement a compact chronological timeline using runtime-supplied
  event labels and sequence.
- V2.19.S4.T3: Implement replay-match display.
- V2.19.S4.T4: Implement divergence display with first sequence, field, expected,
  and actual values when supplied.
- V2.19.S4.T5: Implement stale or missing proof state with disabled export or
  replay action and exact reason.
- V2.19.S4.T6: Add Linux, macOS, and Windows launch/package artifacts according
  to the approved licensing and distribution decision.
- V2.19.S4.T7: Add startup, navigation, update-latency, table-scroll, hidden-page,
  and memory profiling evidence.
- V2.19.S4.T8: Add final fixture, screenshot, keyboard, accessibility, blocked
  flow, no-bypass, and cross-platform release evidence.
- V2.19.S4.T9: Document deferred terminal epics and remove payment/operations
  acceptance from V2.19.

### Acceptance Criteria

- [ ] The replay-match fixture shows runtime-owned hashes, evidence refs,
      timeline, replay `MATCH`, and tamper `CLEAN`.
- [ ] The tamper fixture shows divergence, first differing sequence and field,
      and expected/actual values where supplied.
- [ ] Missing or stale proof state disables unavailable proof actions with an
      exact reason.
- [ ] The UI does not recompute hashes or perform replay substitutions.
- [ ] Linux, macOS, and Windows builds launch the same golden fixtures and
      produce the same semantic states.
- [ ] Startup, command navigation, API update, table scrolling, and memory
      behavior meet the documented budgets on reference hardware or have an
      explicitly accepted exception.
- [ ] Release evidence includes allowed, denied, oversized, unavailable, replay,
      tamper, stale, disconnected, and unknown-command states.

### Conditional Acceptance

**Financial capability and policy**

- [ ] Policy version and outcome remain visible near proof identity.

**Audit and replay**

- [ ] Hashes, evidence refs, replay capsule, status, timeline, and divergence are
      displayed without recomputation.

**Runtime topology and ownership**

- [ ] No runtime topology changes are introduced.

**Model, tool, adapter, or execution boundary**

- [ ] Attribution is display-only; no direct provider or adapter call is
      introduced.

**CaseOps API or UI**

- [ ] Proof data and any governed replay request use documented `tkapi`
      contracts only.

### Evidence Plan

- Demo: allowed case -> `PROOF` -> replay match; tamper fixture -> first
  divergence; stale fixture -> disabled action; repeat on all three platforms.
- Tests: proof models, timeline ordering, replay states, stale state, malformed
  hash fields, inaccessible action, platform launch smoke, and performance
  budgets.
- Artifacts: screenshots, golden fixture payloads, focused test commands,
  profile captures, build logs, package/launch artifacts, and licensing record.

## Conditional Boundary Checklist

| Boundary | Status | Notes |
| --- | --- | --- |
| Financial capability and policy | Applies | Terminal displays outcomes and scope from `tkpoly`; no policy logic moves into Qt. |
| Audit records and evidence | Applies | `PROOF` displays runtime-owned identities, timeline, and evidence refs. |
| Replay and divergence behavior | Applies | `PROOF` displays replay match/divergence from `tkrepl`; Qt does not replay. |
| Runtime topology, tile ownership, or links | N/A | No tile, link, queue, process, or workspace changes. |
| Model gateway governance (`tkmodl`) | Applies | Terminal displays attribution and status only. |
| Tool or adapter dispatch (`tktool` / `tkadpt`) | Applies | Terminal displays attribution; it does not call providers directly. |
| Approved execution (`tkexec`) | N/A | Live execution remains disabled; governed paper action may use an existing contract. |
| CaseOps API/UI (`tkapi`) | Applies | All runtime reads and governed actions pass through `tkapi`. |
| Storage roles | Applies | Terminal does not connect directly to Markdown, DuckDB, TigerBeetle, audit files, or replay files. |
| Metrics and diagnostics | Limited | Only compact availability/source state is shown; full operations UI is deferred. |
| Security and fail-closed behavior | Applies | Missing, stale, malformed, denied, or disconnected state cannot appear actionable. |
| Cross-platform desktop | Applies | Linux, macOS, and Windows build, launch, input, scaling, and fixture parity are release gates. |
| Qt licensing and distribution | Applies | An approved decision record is required before package release. |

## Epic Acceptance

- [ ] One Qt 6 desktop application builds and launches on Linux, macOS, and
      Windows.
- [ ] One command launches the terminal against deterministic local fixtures.
- [ ] The default USD 2,000 case shows thesis, normalized mandate, account,
      basket, ticket, rejected candidates, policy summary, compact impact,
      proposal identity, and replay status.
- [ ] Restricted `SOXL` is visible with its exact denial and no inappropriate
      action affordance.
- [ ] The USD 25,000 case shows the effective maximum or blocking limit, exact
      reason, and disabled action.
- [ ] `POLICY`, `IMPACT`, `PROOF`, and `SYSTEM` are reachable by keyboard and
      command.
- [ ] Missing, stale, malformed, disconnected, or unavailable data never renders
      as allowed or actionable.
- [ ] The only enabled mutation is an existing governed save-proposal or paper
      action contract; otherwise the terminal is read-only.
- [ ] Mode, source, freshness, policy outcome, exact reason, consequence, and
      action state remain visible at the minimum supported viewport.
- [ ] Midnight Oni color responsibilities are followed: blue for Tickoni and
      focus, amber for terminal interaction, rare wealth vermilion for brand,
      and semantic colors only for operational state.
- [ ] The terminal communicates exclusively through the typed `tkapi` client
      boundary.
- [ ] The UI does not recompute financial policy, hashes, replay, or governed
      action eligibility.
- [ ] Cross-platform, accessibility, command, blocked-flow, no-bypass,
      performance, screenshot, fixture, and licensing evidence is linked.
- [ ] Payment, full CaseOps, capabilities, adapters, queues, metrics, and
      diagnostic workbenches do not block V2.19 release.

## Release / Evidence Gate

- What can the user or operator do now that they could not before this epic?
  Inspect and operate the governed investment-paper workflow through one native,
  keyboard-first desktop terminal instead of reading separate fixture output,
  audit JSONL, replay output, and diagnostic files.
- What changed from the previous roadmap increments? Existing investment,
  impact, audit, replay, runtime, and cross-platform foundations become one
  coherent professional product surface.
- What is the wow effect? One terminal case shows intent, proposal, rejected
  instruments, exact policy, consequence, proposal identity, and replay proof,
  with immediate keyboard movement into the details.
- Which deterministic flow closes the epic? USD 2,000 allowed, `SOXL` denied,
  USD 25,000 blocked or resized, replay match, and deliberate tamper divergence.
- Which financial checks are enforced? None are newly enforced by the UI. The
  terminal displays governed account, cash, buying-power, market, venue, asset
  class, instrument, notional, concentration, holding-period, and action-state
  checks supplied by the runtime.
- What happens when scope, limit, freshness, evidence, or identity is invalid?
  The terminal shows the exact blocked or unavailable state and disables the
  action.
- Is execution live? No. Live execution is out of scope. The UI may expose an
  existing governed paper action or save-proposal action only.
- Can replay run without external effects? Replay truth is supplied by
  `tkrepl` or deterministic fixtures. The terminal does not invoke external
  providers during replay display.
- Which artifacts prove behavior? Golden fixtures, typed contract examples,
  screenshots, UI/model/command tests, platform launch logs, no-bypass tests,
  performance profiles, and the Qt licensing decision record.

## Deferred Terminal Roadmap

The following work should be separate epics rather than V2.19 release gates:

### Payment And Transfer Terminal

- failed payout and payment exception cases;
- beneficiary, rail, currency, country, retry, amount, and approval checks;
- payment proposal and evidence state;
- combined cash-obligation impact where appropriate.

### Trust And CaseOps Terminal

- full audit explorer;
- replay and proof-bundle workflows;
- cross-case review;
- CaseOps queues and human-review state;
- export and external reviewer workflows.

### Runtime Operations Console

- capability catalog;
- adapter and model availability;
- tile, process, queue, link, backpressure, metric, and diagnostic state;
- supervisor and sandbox status;
- operator-only runtime controls under separate authority.

### Later Professional Terminal Work

- saved multi-window workspaces;
- detachable panels;
- research and market-data functions;
- alerts and monitored theses;
- charting based on governed data contracts;
- plugin or extension governance;
- enterprise deployment, update, entitlement, and support tooling.
