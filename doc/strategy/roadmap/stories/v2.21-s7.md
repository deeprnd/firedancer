<!--
Tickoni story issue template.

Story: V2.21.S7
Epic: V2.21

Used for: Retail runtime trust, security, docs, and evidence gate

Required for every story:
  - Product outcome
  - Actor/user story
  - Acceptance criteria
  - Evidence and quality gate tasks as child task issues after the story is
    Ready

Read before filling:
  - doc/strategy/templates/status-template.md for status definitions
  - doc/strategy/README.md for product identity, supported workflows, and
    non-goals
  - doc/knowledge/architecture.md for the runtime model and source-of-truth
  - doc/execution/contribution/tickoni.md for Zig runtime style
  - doc/execution/development.md for build/run commands
  - doc/execution/testing-tickoni.md and doc/execution/ci.md for test selection
  - doc/execution/security.md for fail-closed behavior
  - doc/execution/observability.md and doc/execution/telemetry.md for metrics
-->

# V2.21.S7: Retail runtime trust, security, docs, and evidence gate

**Status:** Backlog
**Epic:** #[github-epic-issue]
**Parent roadmap item:** V2.21: Cross-Platform Retail Runtime Support
**Labels:** `story`, `security`, `enhancement`

Close the epic by updating docs, evidence requirements, operator-visible trust
surfaces, security defaults, and release gates for cross-platform retail
support.

## Product Outcome

The retail runtime can be promoted only when users, operators, and reviewers
can inspect platform support, degraded guarantees, audit/replay evidence, and
no-bypass behavior from product docs and runtime outputs.

## User Story

As a security reviewer, I want cross-platform support to publish explicit
evidence and non-goals, so that portability does not hide weaker controls or
ambiguous financial authority.

## Scope

- In scope: update product/developer/security docs with support matrix,
  runtime tiers, install paths, demo commands, storage locations, manual
  verification, update/uninstall behavior, unsupported features; expose platform
  tier and degraded guarantees in CLI, CaseOps, audit, replay, metrics,
  diagnostics; document paper/sandbox default and no-live-effect for Mac/Win;
  document privacy/telemetry defaults; link evidence artifacts.
- Out of scope: tier definitions (S1), build scope (S2), preflight (S3),
  macOS implementation (S4), Windows implementation (S5), demo execution
  (S6).

## Preconditions And Assumptions

- S1 defines tiers; S3 defines preflight; S4/S5 define platform paths;
  S6 defines demo/conformance.
- S7 is the closure story — it does not introduce new behavior but
  documents and gates everything that came before.

## Acceptance Criteria

- [ ] Given a retail runtime promotion, when reviewers inspect docs, then they
      find the support matrix, runtime tiers, install paths, demo commands,
      storage locations, manual verification, update/uninstall behavior, and
      unsupported features for each supported OS.
- [ ] Given the CLI, CaseOps, audit, replay, metrics, or diagnostics, when
      platform tier is active, then tier and degraded-guarantee information is
      visible where it affects trust.
- [ ] Given consumer Mac/Windows modes, when docs describe execution defaults,
      then they state that paper, fixture, mock, or sandbox-substitute behavior
      is default and live execution is disabled.
- [ ] Given consumer Mac/Windows modes, when docs describe privacy defaults,
      then they state that installer telemetry is disabled unless a later
      explicit privacy decision approves opt-in diagnostics.
- [ ] Given a retail demo completion, when evidence is linked from this epic,
      then support matrix, version samples, doctor samples, installer
      verification samples, demo output, audit JSONL, replay capsule,
      blocked-flow output, and conformance result are all linked.

### Conditional Acceptance

**Financial capability and policy**

- [ ] N/A - story documents existing policy; does not change outcomes.

**Audit and replay**

- [ ] Given a retail demo, when evidence is collected, then audit JSONL and
      replay capsule samples are produced and linked.

**Runtime topology and tile ownership**

- [ ] N/A - story documents tier and platform decisions; topology is in S1.

**Model, tool, adapter, or execution boundary**

- [ ] N/A - story documents that model/tool/adapter/execution boundaries remain
      enforced in retail modes.

**CaseOps API or UI**

- [ ] Given CaseOps, when platform tier is active, then tier and degraded-
      guarantee information is visible.

## Child Task Issues

V2.21.S7.T1 — Architecture and planning. Identify which product, developer,
and security docs need updating for the retail runtime support tiers. Identify
risks, known unknowns, and documentation decisions that must be resolved. Do
not start implementation until this task blocks no remaining implementation task.

V2.21.S7.T2 — Domain-Driven Design and scaffolding. Define the structs for
evidence linking, support matrix data, and trust-surface exposure. Implement
scaffold functions with `NotImplemented` errors and `log.warn` calls only.

V2.21.S7.T3 — Test-Driven Design: write tests against the scaffolding from T2.
Then lightly implement the scaffolding methods with correct hardcoded return
values and a `// TODO: implement` comment. All tests must pass before
implementation tasks start.

V2.21.S7.T4 — Documentation and DevOps sweep. Update `doc/knowledge/`,
`doc/execution/`, and `doc/strategy/` docs to reflect retail runtime trust
decisions. Update roadmap status.

V2.21.S7.T5 — Update user-facing docs: support matrix, runtime tiers,
install paths, demo commands, storage locations, manual verification,
update/uninstall behavior, unsupported features.

V2.21.S7.T6 — Update developer docs: platform-specific build prerequisites,
narrow test commands, CI expectations, build-scope rules.

V2.21.S7.T7 — Ensure CLI, CaseOps, audit, replay, metrics, and diagnostics
expose platform tier and degraded guarantees.

V2.21.S7.T8 — Document paper/sandbox default and no-live-effect for consumer
Mac/Windows modes.

V2.21.S7.T9 — Document privacy and telemetry defaults.

V2.21.S7.T10 — Link evidence artifacts: support matrix, version/doctor
samples, installer verification, demo output, audit JSONL, replay capsule,
blocked-flow output, conformance result.

V2.21.S7.T11 — Security audit. Audit the story's code against
`doc/execution/security.md`.

V2.21.S7.T12 — Telemetry and observability audit. Audit against
`doc/execution/telemetry.md` and `doc/execution/observability.md`.

V2.21.S7.T13 — Evidence and quality gate. Run the full closing gate per
`doc/execution/quality.md`.

## Evidence Plan

- Demo or command: N/A - documentation and closure story.
- Tests: N/A.
- Fixtures or samples: All evidence artifacts from S3, S4, S5, S6.
- Audit/replay evidence: Audit JSONL and replay capsule samples.
- Blocked-flow evidence: Blocked-flow diagnostic from S6.

## Quality Gate

- [ ] Product, developer, and security docs explain the support tiers and do
      not imply throughput or isolation guarantees the selected mode does not
      provide.
- [ ] Evidence artifacts prove a successful retail demo and at least one
      fail-closed unsupported or tampered flow on supported macOS and Windows
      paths.
- [ ] Non-goals and deferred work are visible, including native full-runtime
      parity.
- [ ] Documentation and roadmap status are updated.

## Notes And Open Questions

- (None at this time — the closure story surfaces any remaining open questions
  from S1-S6.)
