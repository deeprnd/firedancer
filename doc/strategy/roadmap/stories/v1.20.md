# V1.20: Guarded Crypto Transfers

**Status:** Backlog
**Milestone:** M7
**Labels:** `epic`, `crypto`, `security`, `audit`, `trust`, `enhancement`

Build on V1.5 crypto/stablecoin guardrails and V1.9 guarded spot trading to
turn approved custody or exchange balances into policy-checked crypto transfer
proposals, then paper-run or submit only explicitly approved sandbox transfers
through governed adapter and execution boundaries.

The blockchain or settlement network remains TBD. Candidate networks are
popular L1 or L2 rails selected through an explicit product, policy, risk, and
adapter decision before implementation. This epic must not silently choose a
chain, bridge, token contract, wallet format, fee model, or execution authority.

## Product Intent

Users should be able to move crypto or stablecoin value only after Tickoni can
prove the source account, asset, network, wallet, amount, fee, chain-risk,
travel-rule, approval, adapter, and replay boundaries. A guarded spot trade in
V1.9 may create an eligible balance, but trading authority still does not grant
transfer authority.

V1.20 closes the next crypto-money step:

```text
guarded spot position or custody balance
  -> transfer intent
  -> wallet/network/asset policy
  -> fee-aware transfer proposal
  -> approval-bound paper or sandbox transfer
  -> read-back, audit, and replay
```

## Users And Jobs

- Crypto or stablecoin user: request a transfer or withdrawal and see whether
  the wallet, network, asset, amount, fee, and evidence are allowed before any
  money can move.
- CaseOps operator: review a transfer proposal, blocked reasons, approval
  state, signed envelope, read-back status, audit timeline, and replay result.
- Compliance or risk reviewer: inspect chain-risk, travel-rule, wallet
  allowlist, counterparty, source-of-funds, and jurisdiction evidence before
  approving or denying the transfer.
- Runtime owner: verify that adapters, execution, audit, replay, metrics, and
  diagnostics prove no direct wallet, exchange, bridge, or custody bypass.

## Success Metrics

- A deterministic local flow turns a custody or post-trade balance into a
  transfer proposal with source account, asset, network, destination wallet,
  amount, fee estimate, risk evidence, policy outcome, and proposal hash.
- Unsupported networks, unsupported assets, wallet mismatches, stale fee
  estimates, missing travel-rule evidence, high chain-risk, excessive amount,
  duplicate action id, expired approval, and direct adapter bypass fail closed.
- Trading capabilities from V1.9 cannot satisfy `crypto_transfer.propose`,
  `crypto_transfer.prepare`, or `crypto_transfer.initiate`.
- Paper mode remains the default. Sandbox transfer submission requires an
  explicitly enabled signed adapter manifest, policy allow or required approval,
  exact proposal hash binding, deterministic action id, and read-back
  reconciliation.
- Replay reproduces transfer proposal, policy outcome, approval state,
  submission substitute, read-back result, and final state without live wallet,
  chain, exchange, bridge, model, adapter, or execution side effects.

## Demo Moment

- Command or flow: run a deterministic local CaseOps or CLI flow for a USDC
  transfer from an approved custody account to an allowlisted wallet on the
  selected TBD sandbox network fixture.
- Expected result: the operator sees an allowed or approval-required transfer
  proposal, a paper or sandbox submission state when explicitly enabled, exact
  fee and destination evidence, blocked examples, append-only audit output, and
  replay match with external effects disabled.

## Scope

### In Scope

- Crypto transfer intent schema for withdrawals, internal custody moves,
  exchange-to-wallet moves, wallet-to-custody deposit reconciliation, and
  stablecoin cash movement.
- Candidate network decision record for one initial popular L1 or L2 sandbox
  rail, with explicit unresolved dimensions before implementation.
- Source custody or exchange account snapshot, spendable balance, locked
  balance, pending transfer, and post-transfer balance representation.
- Destination wallet allowlist, network allowlist, asset allowlist,
  counterparty allowlist, memo/tag/payment-id handling where applicable, and
  address-format validation fixtures.
- Fee and gas estimate capture with freshness, max fee, native gas balance,
  fee payer, and final amount checks.
- Chain-risk, sanctions, travel-rule, jurisdiction, and source-of-funds
  evidence gates where configured by policy.
- `crypto_transfer.propose`, `crypto_transfer.prepare`, and approved sandbox
  `crypto_transfer.initiate` capability checks with explicit policy outcomes.
- Adapter reads and sandbox submission through `tktool`, `tkadpt`, and future
  approved `tkexec` boundaries only; no direct agent or UI wallet access.
- Append-only audit, evidence, metrics, diagnostics, and replay samples for
  allowed, approval-required, blocked, tampered, and bypass-attempt flows.

### Out Of Scope

- No production live crypto transfers by default.
- No autonomous transfer execution, withdrawal approval, custody policy
  override, or wallet key access for agents or UI.
- No bridge transfers, DeFi swaps, staking, lending, yield strategies, cross-
  chain routing, token wrapping, or contract interaction beyond simple transfer
  fixtures.
- No final blockchain, wallet provider, custody provider, exchange, bridge, or
  L2 selection without an explicit product and architecture decision.
- No direct TigerBeetle, DuckDB, Markdown, model-provider, wallet, chain RPC,
  exchange, custody, or bridge access from agents or CaseOps UI.
- No reuse of Solana validator schemas, RPC semantics, or validator tile
  identities as Tickoni crypto transfer semantics.

## Conditional Boundary Checklist

| Boundary | Status | Notes / linked story |
| --- | --- | --- |
| Financial capability and policy | Applies | V1.20.S4 owns transfer-specific envelope and policy checks. |
| Audit records and evidence | Applies | V1.20.S6 owns transfer audit, evidence, and proof artifacts. |
| Replay and divergence behavior | Applies | V1.20.S6 owns replay substitution and tamper cases. |
| Runtime topology, tile ownership, or links | Decision needed | No new tile ownership is assumed; any topology change needs a separate decision. |
| Model gateway governance (`tkmodl`) | N/A | Transfer execution must not require live model calls; model outputs are substituted in replay when present. |
| Tool broker or adapter dispatch (`tktool` / `tkadpt`) | Applies | V1.20.S2 and V1.20.S5 route reads and sandbox adapter calls through governed paths. |
| Approved execution (`tkexec`) | Applies | V1.20.S5 covers paper/default and approved sandbox execution boundaries only. |
| CaseOps API/UI (`tkapi`) | Applies | V1.20.S6 covers review, approval, blocked-flow, and proof visibility. |
| Storage role: Markdown, DuckDB, TigerBeetle | Applies | Evidence may reference Markdown policy context and DuckDB analytics; neither becomes transfer ledger truth. |
| Metrics, diagnostics, and operations | Applies | V1.20.S6 covers adapter, queue, replay, denial, and read-back signals. |
| Security and fail-closed behavior | Applies | All stories must prove no direct keys, no direct adapter bypass, and denied-by-default mutation. |

## Story Breakdown

- [ ] V1.20.S1: Transfer intent and candidate network decision - #[github-story-issue]
- [ ] V1.20.S2: Custody, wallet, network, and fee snapshots - #[github-story-issue]
- [ ] V1.20.S3: Fee-aware transfer proposal and signed envelope - #[github-story-issue]
- [ ] V1.20.S4: Crypto transfer capability and compliance checks - #[github-story-issue]
- [ ] V1.20.S5: Paper and approved sandbox transfer lifecycle - #[github-story-issue]
- [ ] V1.20.S6: Transfer demo, audit, replay, and no-bypass proof - #[github-story-issue]

## WBS

### V1.20.S1: Transfer intent and candidate network decision

**Status:** Backlog
**Labels:** `story`, `crypto`, `enhancement`

**Description:** Define transfer intent, preserve trading/transfer authority
separation, and record the chain/network decision inputs before selecting one
popular L1 or L2 sandbox rail.

Tasks:

- V1.20.S1.T1: Define a bounded crypto transfer intent schema with actor,
  workflow, case or run id, source account, transfer type, asset, amount,
  destination wallet or custody account, requested network, counterparty,
  environment, memo/tag where applicable, and user exclusions.
- V1.20.S1.T2: Normalize plain-English withdrawal, internal move,
  exchange-to-wallet, wallet-to-custody reconciliation, and stablecoin cash
  movement requests into that schema.
- V1.20.S1.T3: Reject missing amount, ambiguous asset identity, missing
  destination, unsupported transfer type, conflicting network instructions,
  unsupported memo/tag requirements, and ticker/token/network collisions before
  any adapter call.
- V1.20.S1.T4: Create a candidate network decision record for popular L1 or L2
  sandbox candidates, covering wallet format, token contract model, fee asset,
  finality assumptions, chain-risk coverage, travel-rule support, adapter
  availability, replay substitution, and sandbox feasibility.
- V1.20.S1.T5: Keep asset identity, network identity, token-contract identity,
  custody account identity, and destination wallet identity distinct.

Acceptance:

- A user request becomes deterministic transfer intent without requiring the
  user to know internal wallet ids or network ids.
- The epic cannot proceed to implementation with an implicit chain choice;
  unsupported or ambiguous network instructions fail closed.

### V1.20.S2: Custody, wallet, network, and fee snapshots

**Status:** Backlog
**Labels:** `story`, `crypto`, `enhancement`

**Description:** Capture deterministic transfer inputs: source balances,
destination wallet allowlists, network status, fee estimates, and compliance
evidence through governed read paths only.

Tasks:

- V1.20.S2.T1: Define a custody or exchange account snapshot with account id,
  environment, provider, asset balances, reserved balances, pending transfers,
  day/month transfer usage, native gas balance where applicable, and snapshot
  identity.
- V1.20.S2.T2: Define wallet and counterparty fixtures with wallet id, network,
  address, asset compatibility, owner/counterparty id, allowlist status,
  jurisdiction, travel-rule status, sanctions status, and evidence hashes.
- V1.20.S2.T3: Define network and fee snapshots with chain id or network id,
  status, finality model, fee asset, estimated fee, max fee, gas or resource
  requirement, timestamp, sequence, and evidence hash.
- V1.20.S2.T4: Validate wallet format, network status, asset-network
  compatibility, positive balances, spendable amount after fee, memo/tag
  requirements, quote freshness, and monotonic snapshot identity before use.
- V1.20.S2.T5: Route account, wallet, network, chain-risk, and fee reads
  through `tktool` and `tkadpt`; agents and UI code receive no wallet,
  custody, exchange, chain RPC, or provider credentials.

Acceptance:

- Transfer proposals use identified source, wallet, network, fee, and evidence
  snapshots.
- Malformed addresses, unsupported networks, insufficient gas, stale fees,
  missing evidence, or mismatched asset/network pairs fail closed with exact
  blocked reasons and no transfer proposal.

### V1.20.S3: Fee-aware transfer proposal and signed envelope

**Status:** Backlog
**Labels:** `story`, `crypto`, `enhancement`

**Description:** Build deterministic transfer proposals, compute amount and fee
effects, hash-bind every material input, and prepare signed envelopes without
executing by default.

Tasks:

- V1.20.S3.T1: Build a deterministic transfer proposal from intent, source
  snapshot, wallet fixture, network snapshot, fee estimate, compliance
  evidence, and policy version.
- V1.20.S3.T2: Define transfer proposal fields: source account, destination,
  asset, network, amount, fee asset, estimated fee, max fee, net received,
  memo/tag, counterparty, transfer type, approval requirement, and expiration.
- V1.20.S3.T3: Apply decimal precision, minimum transfer size, dust threshold,
  max fee, spendable balance, native gas balance, and post-transfer reserve
  rules deterministically.
- V1.20.S3.T4: Prepare a signed or signable action envelope only after policy
  allows preparation; the envelope must include proposal hash, capability
  envelope, policy version, approval state, action id, idempotency key, and
  adapter manifest identity.
- V1.20.S3.T5: Hash-bind every proposal and envelope to transfer intent,
  source snapshot, wallet fixture, network snapshot, fee estimate, evidence,
  policy version, and capability envelope.

Acceptance:

- Proposal amount plus fees fit spendable balance and configured reserves.
- Any changed destination, network, amount, fee, evidence, policy, approval, or
  adapter manifest invalidates the prepared envelope before submission.

### V1.20.S4: Crypto transfer capability and compliance checks

**Status:** Backlog
**Labels:** `story`, `security`, `enhancement`

**Description:** Evaluate crypto transfers through `tkpoly`, enforce wallet,
network, asset, amount, chain-risk, travel-rule, approval, and authority
separation checks, and return explicit policy outcomes.

Tasks:

- V1.20.S4.T1: Evaluate transfers through `tkpoly` using
  `crypto_transfer.propose`, `crypto_transfer.prepare`, and approved sandbox
  `crypto_transfer.initiate` capability envelopes.
- V1.20.S4.T2: Check actor, role, workflow, case or run id, source account,
  custody provider, environment, transfer type, asset, network, destination
  wallet, counterparty, amount, fee, policy version, and budget id where
  applicable.
- V1.20.S4.T3: Enforce configured per-transfer, per-day, per-month,
  per-wallet, per-asset, per-network, source-account, fee, frequency, and
  exposure limits.
- V1.20.S4.T4: Enforce wallet allowlist, network allowlist, asset allowlist,
  restricted token, restricted destination, chain-risk, sanctions, travel-rule,
  jurisdiction, source-of-funds, memo/tag, and evidence requirements.
- V1.20.S4.T5: Return explicit `allow`, `deny`, `require_approval`,
  `require_more_evidence`, or `escalate` outcomes with failed scope dimensions
  and user-readable reasons.
- V1.20.S4.T6: Prove that `trading_order.place` or V1.9 spot trading approval
  cannot satisfy `crypto_transfer.initiate`, and that transfer approval cannot
  place a trade.

Acceptance:

- Unsupported assets, unsupported networks, unsafe wallets, high-risk chain
  signals, missing travel-rule evidence, excessive fees, insufficient balance,
  over-limit transfers, and stale approvals are rejected before preparation or
  submission.
- Trading and transfer capabilities cannot substitute for each other.

### V1.20.S5: Paper and approved sandbox transfer lifecycle

**Status:** Backlog
**Labels:** `story`, `crypto`, `enhancement`

**Description:** Keep paper mode as default, submit only approved sandbox
transfers through governed boundaries, reconcile read-back, and distinguish
pending, accepted, broadcast, confirmed, rejected, and mismatch states.

Tasks:

- V1.20.S5.T1: Keep paper mode as default and require an explicitly enabled
  signed adapter manifest plus environment allowlist for sandbox submission.
- V1.20.S5.T2: Bind submission to the exact validated proposal hash, prepared
  envelope, policy decision, approval where required, source account, wallet,
  network, asset, amount, fee, action id, idempotency key, and adapter manifest.
- V1.20.S5.T3: Support paper transfer lifecycle and approved sandbox adapter
  states for pending, accepted, broadcast, confirmed, rejected, expired,
  cancelled, and read-back mismatch.
- V1.20.S5.T4: Reconcile read-back source account, destination, network, asset,
  amount, fee, transaction id or sandbox reference, status, action id, and
  idempotency key against the submitted envelope.
- V1.20.S5.T5: Update captured balance or portfolio view only from accepted
  confirmation or deterministic paper evidence; never infer transfer success
  from submission success alone.
- V1.20.S5.T6: Reject duplicate action ids, expired or revoked approvals,
  changed snapshots, changed fees, changed wallets, changed networks, direct
  adapter calls, provider mismatches, and kill-switch activation before
  submission.

Acceptance:

- An allowed transfer reaches paper or approved sandbox state only when every
  bound identity matches exactly.
- Submission, broadcast, confirmation, rejection, expiry, cancellation, and
  read-back mismatch are distinguishable, audited states.

### V1.20.S6: Transfer demo, audit, replay, and no-bypass proof

**Status:** Backlog
**Labels:** `story`, `audit`, `enhancement`

**Description:** Add deterministic demo and evidence for a guarded crypto
transfer, blocked scenarios, audit output, replay substitution, tamper
detection, metrics, diagnostics, and no direct wallet or adapter bypass.

Tasks:

- V1.20.S6.T1: Add one deterministic local demo for an approved stablecoin or
  crypto transfer using captured intent, source account, wallet, network, fee,
  policy, approval, tool, adapter, and execution fixtures.
- V1.20.S6.T2: Show eligible and rejected wallets, networks, assets, fees,
  compliance evidence, policy outcomes, proposal hash, approval state, action
  id, paper or sandbox lifecycle, and post-transfer balance effect.
- V1.20.S6.T3: Include blocked unsupported-network, unsupported-asset,
  wallet-not-allowlisted, stale-fee, insufficient-gas, high-chain-risk,
  missing-travel-rule, over-limit, duplicate-action-id, expired-approval, and
  direct-adapter-bypass scenarios.
- V1.20.S6.T4: Emit append-only audit, metrics, diagnostics, and evidence for
  intent, snapshots, fee estimate, chain-risk, policy, proposal, approval,
  envelope, action, adapter substitute, read-back, denial, and replay events.
- V1.20.S6.T5: Replay from captured inputs with model, wallet, chain RPC,
  custody, exchange, bridge, adapter, and execution effects structurally
  disabled.
- V1.20.S6.T6: Report the first divergent intent, wallet, network, fee,
  policy, approval, envelope, action, read-back, or balance hash and include
  one deliberate tamper case.

Acceptance:

- One demo produces a guarded paper or approved sandbox transfer and exact
  evidence for every blocked scenario without live model, wallet, chain,
  exchange, bridge, custody, or transfer effects.
- Replay reproduces proposal values, policy outcomes, approval state, transfer
  lifecycle state, fees, read-back results, and final captured balance state,
  and detects the tampered fixture.

## Epic Acceptance

- [ ] All required story issues are done or explicitly deferred with rationale.
- [ ] The final initial blockchain/network choice is linked to an explicit
      decision record before implementation or sandbox submission.
- [ ] The demo moment succeeds using deterministic local inputs.
- [ ] Transfer intent, wallet, network, fee, compliance, proposal, approval,
      action, read-back, audit, replay, metrics, and diagnostics artifacts are
      linked from this epic.
- [ ] Non-goals and deferred work are visible in the roadmap.
- [ ] No live model, wallet, chain RPC, exchange, bridge, custody, TigerBeetle,
      or execution side effects occur unless a separate approved sandbox/live
      execution path explicitly enables them.

## Release / Evidence Gate

- What can the user or operator do now that they could not before this epic?
  They can create, review, approve, paper-run, or explicitly sandbox-submit a
  guarded crypto or stablecoin transfer with visible wallet, network, fee,
  compliance, and replay evidence.
- What changed from the previous roadmap increment?
  V1.9 guarded exchange spot trades; V1.20 keeps that authority separate and
  adds wallet/network/custody transfer controls.
- What is this epic's wow-effect?
  A user can ask to move crypto, and Tickoni shows the exact destination,
  chain, fee, compliance, approval, and no-bypass proof before any transfer can
  occur.
- How does this epic progress Tickoni's unique value proposition?
  It extends policy-gated action and forensic replay from crypto trading into
  crypto money movement while preserving consequence isolation.
- Which demo command or CaseOps flow closes the epic?
  A deterministic local CaseOps or CLI flow for an approved stablecoin or
  crypto transfer plus blocked wallet/network/fee/compliance scenarios.
- Which checks are enforced?
  Source account, custody provider, wallet, network, asset, token contract
  where applicable, counterparty, amount, fee, native gas balance, transfer
  frequency, chain-risk, travel-rule, jurisdiction, evidence, approval, and
  adapter manifest checks.
- What happens when requested scope exceeds policy or evidence?
  `tkpoly` returns `deny`, `require_approval`, `require_more_evidence`, or
  `escalate`; no proposal preparation or submission occurs beyond the allowed
  state.
- Is execution paper-only, draft-only, sandbox, live, or disabled?
  Paper is default. Sandbox transfer submission is allowed only through an
  explicitly enabled signed adapter manifest and approval path. Live execution
  is out of scope by default.
- Which fixtures prove behavior?
  Intent, custody account, wallet allowlist, network, fee estimate, chain-risk,
  travel-rule, policy, approval, adapter substitute, read-back, audit JSONL,
  replay capsule, and blocked-flow fixtures.
- Can replay run without external side effects?
  Yes. Replay substitutes model, wallet, chain, custody, exchange, bridge,
  adapter, execution, and read-back outputs.
- What intentional blocked-flow or divergence proves fail-closed behavior?
  Wallet-not-allowlisted, stale-fee, unsupported-network, missing-travel-rule,
  and tampered proposal-hash fixtures.

## Dependencies And Decisions

- Depends on V1.5 crypto/stablecoin guard policy dimensions.
- Builds on V1.6 signed adapter manifest, action id, idempotency, read-back,
  and kill-switch patterns when sandbox submission is enabled.
- Builds on V1.9 crypto spot trading output while keeping trading and transfer
  authority separate.
- Decision needed: initial popular L1 or L2 sandbox network, wallet/address
  format, token-contract scope, fee asset, finality assumptions, chain-risk
  provider fixture, travel-rule fixture, adapter manifest, and read-back shape.
- Decision needed: whether approved sandbox transfer submission is owned by
  existing adapter/execution boundaries or requires a new tile/topology change.
