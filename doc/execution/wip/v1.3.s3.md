# V1.3.S3 Closure

## Product / Architecture

- [x] Wire `drift.zig` into the active allowed-trade runtime/demo path so the branch produces drift outputs outside unit tests.
- [x] Expose a combined interface payload from `investment.zig` that includes both `decision_cards` and `drift_contract`.
- [x] Carry drift proposal state through `tkrepl` replay verification with deterministic rebalance and payment-update hashes.
- [x] Carry drift proposal state through `tkaudt` allowed-trade audit records, including policy and proposal events for rebalance and payment retry updates.
- [x] Enforce `PaymentDriftPolicy.cash_buffer_threshold_cents` inside drift assessment instead of relying on a caller-supplied boolean.
- [x] Expand the payment proposal update contract to include beneficiary, source event, rail, currency, amount, expiry, evidence, approval state, action class, approval path, and policy version.
- [x] Generate meaningful rebalance suggestions, including sell pressure and populated `current_weight_bp`, instead of only buy-or-hold outputs.

## Development

- [x] Reject overlong restricted tickers fail-closed instead of truncating them.
- [x] Add validation for invalid drift inputs such as out-of-range basis points and negative cash-buffer thresholds.
- [x] Correct the stale fixture timestamp comment in `src/tickoni/schema/drift.zig`.
- [x] Restore clean formatting so `zig fmt check` passes for the updated drift implementation.
- [x] Fix build wiring for replay and integration targets so drift-aware tests compile without duplicate-symbol or missing-module issues.

## Testing

- [x] Extend drift unit coverage for sell-side rebalance output, governed payment proposal fields, deterministic hashing, empty-adjustment no-drift state, and fail-closed validation paths.
- [x] Extend integration coverage so the runtime/demo path exposes the drift contract in the rendered decision payload.
- [x] Extend replay coverage to assert rebalance/payment-update hashes and user-action flags from the replay capsule.
- [x] Update tampered replay fixtures so tamper detection still isolates the intended divergent field.
- [x] Regenerate the allowed-trade audit JSONL fixture for the 15-event audit chain.
- [x] Preserve the no-live-effects replay guarantee while adding drift verification.

## Validation

- [x] `zig build test`
- [x] `zig build integration-test`
- [x] `just test-unit-tk`
- [x] `just test-integration-tk`
- [x] `just quality-format-check-tk`
- [x] `just quality-lint-check-tk`
